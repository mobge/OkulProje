@using Proje.ViewModels
@model KullaniciDetailViewModel
@{
    ViewBag.Title = "Guncelle";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<h2>Güncelle</h2>
@using (Html.BeginForm("Guncelle", "Kullanici", FormMethod.Post))
{
    <div class="form-group">
        <label>Sicil No:</label>
        @Html.TextBoxFor(m => m.updatedKullanici.Sicil_No, new { @class = "form-control", @readonly = "readonly", required = "required" })
        @Html.ValidationMessageFor(m => m.updatedKullanici.Sicil_No, "", new { @class = "text-danger" })
    </div>
    <div class="form-group">
        <label>Ad:</label>
        @Html.TextBoxFor(m => m.updatedKullanici.Ad, new { @class = "form-control", required = "required" })
        @Html.ValidationMessageFor(m => m.updatedKullanici.Ad, "", new { @class = "text-danger" })
    </div>
    <div class="form-group">
        <label>Soyad:</label>
        @Html.TextBoxFor(m => m.updatedKullanici.Soyad, new { @class = "form-control", required = "required" })
        @Html.ValidationMessageFor(m => m.updatedKullanici.Soyad, "", new { @class = "text-danger" })
    </div>
    <div class="form-group">
        <label>Şifre:</label>
        @Html.TextBoxFor(m => m.updatedKullanici.Sifre, new { @class = "form-control", required = "required" })
        @Html.ValidationMessageFor(m => m.updatedKullanici.Sifre, "", new { @class = "text-danger" })
    </div>
    <div class="form-group" style="height:100% !important;">
        <label>Rol Seçiniz:</label>
        @Html.DropDownListFor(m => m.updatedKullanici.Rol_Id, new SelectList(Model.Roller, "Rol_Id", "Rol_Adi", Model.updatedKullanici.Rol_Id), "Seçiniz", new { @class = "form-control", style = "height:100%", required = "required" })
        @Html.ValidationMessageFor(m => m.updatedKullanici.Rol_Id, "", new { @class = "text-danger" })
    </div>
    <button type="submit" class="btn btn-primary">Kaydet</button>
    <span style="color:red;">@ViewBag.Mesaj</span>
}

